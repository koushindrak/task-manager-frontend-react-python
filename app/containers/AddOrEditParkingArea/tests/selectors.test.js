// import { fromJS } from 'immutable';
// import { selectAddOrEditParkingAreaDomain } from '../selectors';

describe('selectAddOrEditParkingAreaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
